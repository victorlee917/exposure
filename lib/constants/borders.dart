import 'package:flutter/material.dart';

class Borders {
  static const buttonRadius = 16.0;
  static const buttonWidth = 2.0;
  static const buttonColorDefault = Color.fromARGB(36, 0, 0, 0);
  static const lineWidth = 1.0;
  static const lineColorDefault = Color.fromARGB(36, 0, 0, 0);
}
